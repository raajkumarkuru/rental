<?php

/**
 * @file
 * Custom Rental Form module.
 */

/**
 * Implements hook_menu().
 */
function custom_rental_form_menu() {
  $items = [];
  $items['rent-property'] = [
    'title' => 'Create Rental Transaction',
    'page callback' => 'custom_rental_form_page',
    'access arguments' => ['access content'],
    'type' => MENU_CALLBACK,
  ];
  return $items;
}
